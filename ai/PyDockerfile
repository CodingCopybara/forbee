# FROM python:3.9-slim

# RUN apt-get update && apt-get install -y \
#     libglib2.0-0 \
#     libsm6 \
#     libxext6 \
#     libxrender-dev \
#     libgomp1 \
#     libglib2.0-0 \
#     libgtk-3-0 \
#     libavcodec-dev \
#     libavformat-dev \
#     libswscale-dev \
#     && rm -rf /var/lib/apt/lists/*

# WORKDIR /python

# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt

# COPY . .

# RUN mkdir -p results

# EXPOSE 8003

# ENV PYTHONPATH=/python
# ENV PYTHONUNBUFFERED=1

# CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8003"]


# 버전 정해야 함 
FROM python:3.12-slim
WORKDIR /python

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8003"]


# .dockerignore 파일 생성 
# Java 관련 파일 추가 (/src/main/java, /src/main/resources, /target, pom.xml)
# docker build -t <이미지 이름> -f PyDockerfile .
# docker run -d -p 8003:8003 --name <컨테이너 이름> <이미지 이름>