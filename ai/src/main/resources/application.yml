server:
  port: 8083

---

spring:
  config:
    activate:
      on-profile: default
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://forbee-ai-db.mysql.database.azure.com:3306/ai?useSSL=true&requireSSL=false
    username: forbeeai
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: ai
      auto-offset-reset: earliest
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9090/realms/master/protocol/openid-connect/certs

  # cloud:
  #   stream:
  #     kafka:
  #       binder:
  #         brokers: localhost:9092
  #       streams:
  #         binder:
  #           configuration:
  #             default:
  #               key:
  #                 serde: org.apache.kafka.common.serialization.Serdes$StringSerde
  #               value:
  #                 serde: org.apache.kafka.common.serialization.Serdes$StringSerde
  #     bindings:
  #       event-in:
  #         group: ai
  #         destination: forbee
  #         contentType: application/json
  #       event-out:
  #         destination: forbee
  #         contentType: application/json

azure:
  storage:
    account-url: ${AZURE_STORAGE_ACCOUNT_URL:https://forbee.blob.core.windows.net}
    container-name: ${AZURE_CONTAINER_NAME:blob}

logging:
  level:
    root: INFO
    org.hibernate.type: trace
    org.springframework.cloud: debug

---

spring:
  config:
    activate:
      on-profile: docker
  main:
    allow-bean-definition-overriding: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://forbee-ai-db.mysql.database.azure.com:3306/ai?useSSL=true&requireSSL=false
    username: forbeeai@forbee-ai-db
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
  kafka:
    bootstrap-servers: kafka:9092
  security:
    oauth2:
      resourceserver:
        jwt:
          # jwk-set-uri: http://localhost:9090/realms/master/protocol/openid-connect/certs
          jwk-set-uri: http://oauth:9090/
  # cloud:
  #   stream:
  #     kafka:
  #       binder:
  #         brokers: my-kafka:9092
  #       streams:
  #         binder:
  #           configuration:
  #             default:
  #               key:
  #                 serde: org.apache.kafka.common.serialization.Serdes$StringSerde
  #               value:
  #                 serde: org.apache.kafka.common.serialization.Serdes$StringSerde
  #     bindings:
  #       event-in:
  #         group: ai
  #         destination: forbee
  #         contentType: application/json
  #       event-out:
  #         destination: forbee
  #         contentType: application/json

python:
  url: http://localhost:8003

azure:
  storage:
    account-url: https://forbee.blob.core.windows.net
    container-name: blob

logging:
  level:
    root: INFO
    # org.hibernate.type: trace
    # org.springframework.cloud: debug